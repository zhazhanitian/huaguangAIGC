# 后端启动修复计划

## 目标
彻底修复当前后端无法启动的问题，使前端 `3002` 能正常代理到后端 `3001`。

## 当前假设
- H1: `server/src/modules/video/video.service.ts` 存在编码损坏字符，导致 TypeScript 解析失败。
- H2: `server/dist/modules/video/video.service.js` 是由损坏源码生成，也包含运行时语法错误。
- H3: 终端显示乱码不是唯一原因，源码本身存在缺失引号/反引号/括号。
- H4: 损坏集中在 `video.service.ts`，其他后端源码可能正常。
- H5: 可通过 git 历史、sourcemap、旧 dist 或上下文恢复出正确源码。

## 阶段
- [x] 阶段 1: 记录运行证据和假设。
- [x] 阶段 2: 添加只读诊断脚本并生成调试日志。
- [x] 阶段 3: 基于日志定位所有语法损坏点。
- [x] 阶段 4: 修复源码而不是只修 dist。
- [x] 阶段 5: 编译验证。
- [x] 阶段 6: 后端启动验证。
- [x] 阶段 7: 前后端联通冒烟验证。
- [x] 阶段 8: 清理临时调试文件并记录根因。

## 前端全模块修复计划

### 目标
检查 `web` 前端项目的每一个模块，修复构建、类型、运行代理和明显源码损坏问题。

### 当前假设
- FH1: 前端可能存在 TypeScript/Vue 类型错误，`npm run build` 会暴露。
- FH2: 后端编码损坏曾影响项目，前端源码也可能存在异常字符或被注释吞掉的代码。
- FH3: 前端 API 代理已恢复，但部分模块可能仍有接口路径、鉴权状态或 websocket 连接问题。
- FH4: 页面/组件模块可能有未导入、未使用或大小写路径问题，在 Windows 上不明显但构建会暴露。
- FH5: 前端没有 lint 脚本，需用构建、静态诊断脚本和冒烟请求组合验证。

### 阶段
- [x] 阶段 9: 收集前端模块结构和入口信息。
- [x] 阶段 10: 运行前端构建/类型检查。
- [x] 阶段 11: 添加只读诊断脚本并生成调试日志。
- [x] 阶段 12: 按证据修复前端源码问题。
- [x] 阶段 13: 重新构建并做前后端冒烟验证。
- [x] 阶段 14: 清理临时调试文件并记录根因。
